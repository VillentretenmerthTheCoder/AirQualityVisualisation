@model IEnumerable<AirQualityVisualisation.Models.Product_Table>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Aktivitet_Table.AktivitetId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Measurement_Table.ProduktId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductType_Table.Navn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proeve_Table.ProduktId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Aktivitet_Table.AktivitetId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Measurement_Table.ProduktId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductType_Table.Navn)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proeve_Table.ProduktId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProduktId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProduktId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProduktId })
        </td>
    </tr>
}

</table>
